/*****************************AutoGenerated Code : Do not edit *******************************/
// Type generated from Swagger definition

{{#if info}}
/*
    {{info.title}} {{info.version}}
    {{info.description}}
    {{#if info.terms}}

    Terms:
    {{info.terms}}
    {{/if}}
    {{#if info.contact}}
    
    Contact:
    {{info.contact.name}}
    {{info.contact.email}}
    {{info.contact.url}}
    {{/if}}
    {{#if info.license}}

    License:
    {{info.license.name}}
    {{info.license.url}}
    {{/if}}
*/
{{/if}}

{{#if tag}}
    {{#each tag}}
    {{this}}
    {{/each}}
{{/if}}


{{#each types}}
    {{#if schema.description}}
    /**
     * {{schema.description}}
     */
    {{/if}}
    {{#if (eq schema.type "array")}}
    export type {{typeName}} = {{fullTypeName}};
    {{else}}
    export {{../generatedTypes}} {{typeName}}  {
    {{#each properties}}
        {{#if (or schema.description schema.example)}}
        /*
         * {{schema.description}}
         {{#if schema.example}}
         * @example {{schema.example}}
         {{/if}}
         */
        {{/if}}
        {{propertyName}}{{../../membersOptional}} : {{typeName}};
    {{/each}}
   
    }
     {{/if}}
{{/each}}